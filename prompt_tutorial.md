# 如何写 Prompt：让 AI 从 Markdown 生成指定排版、带数学公式的 Word 文档

对于很多科研工作者来说，latex 和 markdown 都是常用的写作工具，但word 并不常用，尤其是word中的排版和数学公式让人头大。
本教程主要讨论如何写 prompt，驱动 AI 完成下面这类任务：

- 从 Markdown 生成 `.docx`
- 对齐指定参考版式
- 在 Word 中使用原生数学公式
- 把“模板”和“内容”分离
- 支持批量生成

这里“百科全书条目”只是一个示例场景。 
同样的 prompt 写法也适用于教程、本子等类似任务。

## 主要工作流

我们想要干的事情是：

- 输入是一份或多份参考 `PDF` 样条。先把 PDF 转成图片，便于观察排版细节
- 根据图片逆向工程 Word 的版式结构（如果给的模板文件已经是word格式，则跳过以上步骤）
- 用 Python 脚本把 Markdown 内容渲染成 `.docx`
- 数学公式统一写成 Word 原生公式
- 最后把格式、内容、规范和批量生成能力拆开保存，形成可复用工作流

可以把整个AI工作流概括为：

1. 明确参考样条和目标输出
2. 把 PDF 变成便于观察的图片
3. 提示 AI 根据图片重建版式
4. 提示 AI 把公式升级成 Word 原生公式
5. 提示 AI 把格式与内容分离
6. 提示 AI 产出 Markdown 规则和批量构建脚本
7. 最终形成可复用的 Markdown -> Word 体系

## 从 PDF 样条出发的完整工作流 Prompt

如果你只考虑“从 PDF 样条出发”这一条路线，可以直接使用下面这段完整 prompt。  
首先请安装一个AI agent客户端，比如claude code, codex等等；安装python，事实上还会用到一些python包，但可以让agent自己安装。
然后在PLAN 模式（非常关键）下输入以下prompt。

```text
我希望建立一个可复用的流程：根据参考 PDF 样条，生成指定排版、带数学公式的 Word 文档。

请按下面的要求完成：

1. 先以参考 PDF 为版式来源，不要先猜测 Word 的格式。请先把 PDF 转成图片，用图片观察页面留白、标题区、目录块、章节样式、正文行距、参考文献样式和公式位置。

2. 然后根据这些图片中的版式，逆向工程出 Word 模板效果，并写一个脚本`create_entry.py`，使它生成的 Word 在版式上尽量匹配参考样条。现阶段优先处理格式，不用管内容细节是否最终定稿。

   这个步骤完成后暂停，我需要检查排版效果并微调。

3. 数学公式必须使用 Word 原生公式，不要把公式当普通文本。请同时支持 Markdown 中的行内公式 $...$ 和行间公式 $$...$$，并把它们转换成 Word 数学公式对象。

4. 请特别注意公式排版问题，包括：
   - 行间公式不能被截断
   - 行内公式不能被截断
   - \\cdot 这类符号在 Word 中的显示要尽量自然，不要过大

5. 请把格式和内容分离：create_entry.py 中只保留所有格式信息和渲染逻辑，正文内容放在独立的 Markdown 文件中。之后应支持通过
   `python create_entry.py -f 某个.md -o 输出.docx`
   的方式生成文档。

6. 请把 Markdown 输入规范整理成单独的规则文件markdown_rule.md，明确：
   - front matter 支持哪些字段
   - 章节标题如何写
   - 行内公式和行间公式如何写
   - 参考文献如何写
   - 缺失字段时的默认回退逻辑

7. 请再补充一个批量构建脚本build_all_docx.sh，能够遍历目录下所有 Markdown 文件，调用 create_entry.py 批量生成对应的 Word 文档。

8. 最终结果应包括：
   - 可复用的 create_entry.py
   - 一份 Markdown 规则文件
   - 一个批量生成脚本
   - 至少一个成功生成的 docx 示例

```

额外的说明

- 第1步中会需要装一些把pdf转成图片的包如 pdftoppm

- 第2步中会需要装一些把生成word的包如 python-docx math2docx。 在完成自动排版后，需要手动打开word文档检查排版效果并微调字体大小，行列间距等。例如可以像这样调整：

对word效果截图，然后把图片发送给agent，并输入

```text
[Image #1] 有一些小问题:1.行间公式下半部分看不见,被截掉了 2.\cdot符号看起来好大,跟我通常latex中显示的不一样。请调整。
```

目前 claude code发布了GUI 功能，安装了word MCP 后可以自动完成这一步视觉检查微调的工作，并通过循环的工作流完成自动排版。但依然建议手动检查，以确保排版效果符合预期；另一个要注意的点是不同操作系统（Mac，Windows）以及不同office软件（Microsoft Word，WPS Word）的显示效果可能不同。


- 把markdown 公式转换成word 公式对于人类来说可能很难，但对于agent来说很容易。我们不需要很仔细地告诉agent该怎么做，它自己能搞定。
