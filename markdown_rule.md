# 条目 Markdown 规范

## 文件总体结构

推荐结构：

1. 可选 `YAML front matter`（元数据）
2. 正文内容（标题、段落、公式）
3. 参考文献章节

最小可用结构（无 front matter）也可以：

```md
## 1 基本原理
正文...
```

---

## Front Matter 规则（可选）

使用 `---` 包裹，位于文件最顶部：

```md
---
title: 反向传播算法
pinyin: fanxiang bochuan suanfa
english_name: backpropagation algorithm
alias: 误差反向传播算法
discipline: 数学
summary: 摘要正文...
author: 文再文
toc:
  - 1 基本原理
  - 2 算法推导
  - 3 应用
---
```

### 支持字段

- `title` 或 `标题`
- `pinyin` 或 `拼音`
- `english_name` 或 `英文名称`
- `alias` 或 `又称`
- `discipline` 或 `所属学科`
- `summary` 或 `摘要`
- `author` 或 `作者`
- `toc` 或 `目录`

### 解析细节（重要）

- 这是**受控 YAML 子集**，不是完整 YAML 解析器。
- 支持键值形式：`key: value`
- 支持列表两种写法：
  - 多行列表：
    ```md
    toc:
      - 1 基本原理
      - 2 算法推导
    ```
  - 行内列表：`toc: [1 基本原理, 2 算法推导]`
- front matter 中空行、`#` 注释行会被忽略。
- 值外层的单/双引号会被去掉。
- 不支持嵌套对象（如 `a: {b: 1}`）。

---

## 正文规则

## 标题

使用 Markdown 标题（`#` 到 `######` 都可）：

```md
## 1 基本原理
## 2 算法推导
## 3 应用 
```

说明：
- 脚本把所有级别标题都当“章节标题”处理（视觉样式统一）。
- 目录默认来自这些章节标题（若 front matter 未提供 `toc`）。

## 段落

- 普通文本按段落输出。
- 连续非空行会被拼成同一段（中间不会自动插空格）。
- 以 `- ` 或 `* ` 开头的行会当普通段落处理（不会做 Word 项目符号样式）。

## 行内公式

使用 `$...$`：

```md
误差项定义为 $\delta^{(l)} = \frac{\partial J}{\partial z^{(l)}}$。
```

说明：
- 会转换为 Word 原生公式（OMML）。
- 需要显示美元符号时，用 `\$`。
- 未闭合的 `$` 会按普通文本处理。

## 行间公式

使用 `$$...$$`（单行或多行）：

```md
$$
\delta^{(l)} = (W^{(l+1)})^{T}\delta^{(l+1)} \odot \phi'^{(l)}(z^{(l)})
$$
```

说明：
- 每个行间公式会自动编号（1）（2）（3）...
- 编号按全文出现顺序递增。
- 若你在公式后手写一行 `（2）` 或 `(2)`，脚本会自动忽略该行，避免重复编号。

## 参考文献

创建标题为 `参考文献`（或 `参考资料`）的章节：

```md
## 参考文献
- 文献1...
- 文献2...
```

说明：
- 该章节下内容会按参考文献样式输出。
- 支持普通段落和列表行。

---

## 默认回退逻辑

若 front matter 缺失部分字段，脚本会自动回退：

- `title` 缺失：取正文第一个有标题的章节名；若仍无，则用 md 文件名
- `summary` 缺失：取正文中第一段文本
- `toc` 缺失：取正文章节标题列表
- `author` 缺失：不显示作者行

---

## 完整模板（可直接复制）

```md
---
title: 条目标题
pinyin: pinyin
english_name: english name
alias: 又称（可选）
discipline: 学科名
summary: 这里写摘要（建议1~2句）
author: 作者名,写“文再文”
toc:
  - 1 第一节
  - 2 第二节
  - 3 第三节
---

这里是条目开头正文段落。可包含行内公式 $a^{(L)}=\hat{y}$。

## 1 第一节

正文...

$$
z^{(l)} = W^{(l)}a^{(l-1)} + b^{(l)}
$$

## 2 第二节

正文...

## 3 第三节

正文...

## 参考文献

- 作者A. 文献A.
- 作者B. 文献B.
```

---

## 建议

- 公式尽量使用标准 LaTeX 命令，避免自定义宏。
- 中英文/符号混排时，优先用行内公式而不是 Unicode 上下标字符。
- 一个条目建议控制在约 2 页（可通过精简段落与公式数量调节）。
